const fs = require('fs');
const input = fs.readFileSync("./dev/stdin").toString().trim().split(' ');

function func(arr){
    let sum = 0;
    for(let i = 0; i < n; i++){
        sum += arr[i]
    }
}
func();